#include <iostream>
#include <cmath>

using namespace std;

int main () {

	int a,b,c;
	cin >>a>>b>>c;
	int A = (a*a);
	int B = (b*b);
	int C = (c*c);
	if (A>=0) {
	cout <<A<<endl; }
	if (B>=0) {
	cout <<B<<endl; }
	if (C>=0) {
	cout <<C<<endl; }
	
	return 0;
}