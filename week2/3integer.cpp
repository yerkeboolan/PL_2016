#include <iostream>
#include <cmath>

using namespace std;

int main() {
	
	int x,y,z;
	cin >>x>>y>>z;
        double a = (sqrt(abs(x - 1)) - pow(abs(y, 1/3)))/(1 + ((x*x)/2) + ((y*y)/4));
	double b = x*(atan(z) + pow(e, (-(x+3))));
	if (a != 0 && b == 0) {
	cout <<"The result is " <<" "<<a<<" "<<b; }
	else {       	
	cout <<" "; }
	return 0;
} 